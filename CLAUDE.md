Default to NOT using subagents.

Use subagents only when they provide a clear advantage through
parallelization or isolated context.

Do not use general-purpose subagents for file search, code exploration,
debugging, single-file edits, or straightforward implementation tasks.